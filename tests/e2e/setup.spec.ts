import { test,expect } from "@playwright/test";test("shows honest setup and custody boundaries",async({page})=>{await page.goto("/");await expect(page.getByText("Arc Testnet - no real monetary value")).toBeVisible();await expect(page.getByRole("link",{name:"Finish setup"})).toBeVisible();await page.goto("/wallet");await expect(page.getByText("Controlled by you through Privy")).toBeVisible();await expect(page.getByText("Operated by Arclet through a dedicated Circle Agent Wallet")).toBeVisible();await expect(page.getByRole("button",{name:"Pause without AI"})).toBeVisible()});
