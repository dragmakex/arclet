import { describe,expect,it } from "vitest";import { canonicalHash,canonicalJson } from "@arclet/domain";describe("canonical JSON",()=>{it("sorts object keys recursively without changing array order",()=>{const a={z:[3,2,1],a:{y:true,x:"v"}};const b={a:{x:"v",y:true},z:[3,2,1]};expect(canonicalJson(a)).toBe('{"a":{"x":"v","y":true},"z":[3,2,1]}');expect(canonicalHash(a)).toBe(canonicalHash(b))});it("rejects unsupported values",()=>expect(()=>canonicalJson(1n)).toThrow())});
