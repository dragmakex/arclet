import { describe,expect,it } from "vitest";import { buildQuoteArgs,buildSwapArgs,quoteRequestSchema } from "@arclet/circle";const request=quoteRequestSchema.parse({chain:"ARC-TESTNET",wallet:"0x0000000000000000000000000000000000000001",inputAsset:"USDC",outputAsset:"EURC",inputAtomic:"1000000",inputDecimals:6});describe("Circle command construction",()=>{it("uses a fixed argument array with exact decimal serialization",()=>expect(buildQuoteArgs(request)).toEqual(["wallet","swap","USDC","1","EURC","--chain","ARC-TESTNET","--address","0x0000000000000000000000000000000000000001","--quote","--output","json"]));it("rejects malicious or malformed structured fields",()=>{expect(()=>buildQuoteArgs({...request,inputAsset:"USDC; rm -rf /" as "USDC"})).toThrow();expect(()=>buildSwapArgs({...request,minimumOutputDecimal:"1;echo",slippageBps:50,idempotencyKey:crypto.randomUUID()})).toThrow()});it("binds slippage, minimum output, wallet, chain and idempotency",()=>{const id=crypto.randomUUID();const args=buildSwapArgs({...request,minimumOutputDecimal:"0.99",slippageBps:50,idempotencyKey:id});expect(args).toContain(id);expect(args).toContain("0.99");expect(args).toContain("50")})});
