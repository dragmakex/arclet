import { createArcClient, ARC_USDC, erc20Abi } from "@arclet/chain";import { mkdir,writeFile } from "node:fs/promises";
const client=createArcClient(process.env.ARC_RPC_URL);const chainId=await client.getChainId();if(chainId!==5042002)throw new Error(`Expected Arc Testnet chain 5042002, received ${chainId}`);const block=await client.getBlock();const [decimals,symbol]=await Promise.all([client.readContract({address:ARC_USDC,abi:erc20Abi,functionName:"decimals"}),client.readContract({address:ARC_USDC,abi:erc20Abi,functionName:"symbol"})]);if(decimals!==6||symbol!=="USDC")throw new Error(`Unexpected USDC metadata: ${symbol}/${decimals}`);const evidence={schemaVersion:1,environment:"arc-testnet",chainId,blockNumber:block.number.toString(),blockHash:block.hash,blockTimestamp:block.timestamp.toString(),usdc:{address:ARC_USDC,symbol,decimals},nativeCurrency:{symbol:"USDC",decimals:18},observedAt:new Date().toISOString()};await mkdir("docs/evidence/arc",{recursive:true});await writeFile("docs/evidence/arc/latest.json",JSON.stringify(evidence,null,2)+"\n");console.log(JSON.stringify(evidence,null,2));
