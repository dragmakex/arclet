import { readFile,writeFile } from "node:fs/promises";const manifest=JSON.parse(await readFile("docs/evidence/manifest.json","utf8"));manifest.commit=(await Bun.$`git rev-parse HEAD`.text()).trim();manifest.generatedAt=new Date().toISOString();await writeFile("docs/evidence/manifest.json",JSON.stringify(manifest,null,2)+"\n");console.log("Evidence manifest metadata updated. NOT_RUN statuses were preserved.");
