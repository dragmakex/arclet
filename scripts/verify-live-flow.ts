const allowed=process.env.APP_ENV==="arc-testnet"&&process.env.TRADING_ENABLED==="true"&&process.argv.includes("--confirm-arc-testnet")&&process.argv.some((arg)=>arg.startsWith("--max-spend-usdc="));if(!allowed){console.error("REFUSED: live verification requires APP_ENV=arc-testnet, TRADING_ENABLED=true, --confirm-arc-testnet, and --max-spend-usdc=<tiny amount>.");process.exit(1)}console.error("BLOCKED: the mutation path is not enabled until M0 provider schemas, wallet ownership, and human amount authorization are recorded.");process.exit(2);
