# Third-party software

No project-specific upstream application was imported. Dependencies are installed from the Bun registry lockfile. Exact versions are in `config/dependency-versions.json`.

Primary runtime dependencies: Next.js/React, Privy React and server auth SDKs, viem, Drizzle/Postgres.js, Zod, Decimal.js, Vercel AI SDK and its OpenAI-compatible adapter. Test/build dependencies include Vitest, Playwright, ESLint, TypeScript, and Mermaid CLI. Review package licenses from the lockfile before public submission. The Circle CLI is specified but not installed or vendored.
