import { capabilityStatus } from "../../../../config/runtime";
export const dynamic="force-dynamic";
export default function SettingsPage(){const status=capabilityStatus();return <div className="stack"><div><h1>Setup</h1><p className="muted">Readiness fails closed. Credentials are server-only unless explicitly identified as public.</p></div><section className="panel"><div className="statusline"><h2>Provider configuration</h2><span className="status"><strong>{status.configured?"Configured":"Needs setup"}</strong></span></div>{status.missing.length?<div className="notice">Missing: {status.missing.join(", ")}</div>:<div className="notice">Configuration is present. Run all read-only probes before enabling a market.</div>}</section><section className="grid"><article className="panel"><h2>Human actions</h2><p>Configure Privy origins and email login, supply Graph and source RPC credentials, choose a model, then complete Circle terms and OTP outside Arclet.</p></article><article className="panel"><h2>Safety gates</h2><dl><dt>Trading</dt><dd>{status.tradingEnabled?"Requested":"Disabled"}</dd><dt>Mainnet</dt><dd>Disabled</dd><dt>Enabled markets</dt><dd>0</dd></dl></article></section><section className="panel"><h2>Custody boundary</h2><p>The personal Privy wallet is user-controlled. The Circle trading wallet is application-operated. The signed mandate proves what you approved to Arclet, but it is not an onchain spending-control contract.</p></section></div>}
