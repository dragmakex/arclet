import { getStrategy } from "../../../../lib/repository";import { authenticatedDatabaseUser } from "../../../../lib/request-user";import { safeApiError } from "../../../../lib/http";export async function GET(request:Request,context:{params:Promise<{id:string}>}){try{const {user}=await authenticatedDatabaseUser(request);const strategy=await getStrategy(user.id,(await context.params).id);return Response.json({id:strategy.id,state:strategy.state,version:strategy.current_version,authorizationEpoch:strategy.authorization_epoch,spec:strategy.canonical_spec,strategyHash:strategy.spec_hash})}catch(error){return safeApiError(error)}}
