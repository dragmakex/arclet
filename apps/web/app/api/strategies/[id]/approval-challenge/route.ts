import { createMandateChallenge } from "../../../../../lib/repository";import { authenticatedDatabaseUser } from "../../../../../lib/request-user";import { requireMutationOrigin,safeApiError } from "../../../../../lib/http";export async function POST(request:Request,context:{params:Promise<{id:string}>}){try{const origin=requireMutationOrigin(request);const {identity,user}=await authenticatedDatabaseUser(request);return Response.json(await createMandateChallenge(user.id,identity.embeddedWalletAddress,(await context.params).id,origin),{status:201})}catch(error){return safeApiError(error)}}
