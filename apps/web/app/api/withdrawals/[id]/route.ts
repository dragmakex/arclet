import { authenticatedDatabaseUser } from "../../../../lib/request-user";import { database } from "../../../../lib/repository";import { safeApiError } from "../../../../lib/http";export async function GET(request:Request,context:{params:Promise<{id:string}>}){try{const {user}=await authenticatedDatabaseUser(request);const rows=await database()`SELECT id,state,asset_id,amount_atomic,destination_address,provider_reference,receipt_reference,created_at FROM withdrawals WHERE id=${(await context.params).id} AND user_id=${user.id}`;if(!rows[0])throw new Error("NOT_FOUND");return Response.json(rows[0])}catch(error){return safeApiError(error)}}
