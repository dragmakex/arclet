import { authenticatedDatabaseUser } from "../../../../lib/request-user";import { database } from "../../../../lib/repository";import { safeApiError } from "../../../../lib/http";export async function GET(request:Request,context:{params:Promise<{id:string}>}){try{const {user}=await authenticatedDatabaseUser(request);const rows=await database()`SELECT d.id,d.result,d.reasons,d.proposed_action,d.created_at,o.provenance,o.normalized_metrics,o.quality_verdict FROM decisions d JOIN strategies s ON s.id=d.strategy_id LEFT JOIN market_observations o ON o.id=d.observation_id WHERE d.id=${(await context.params).id} AND s.user_id=${user.id}`;if(!rows[0])throw new Error("NOT_FOUND");return Response.json(rows[0])}catch(error){return safeApiError(error)}}
