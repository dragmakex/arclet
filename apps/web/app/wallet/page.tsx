import { capabilityStatus } from "../../../../config/runtime";
import { SessionControls } from "../../components/session-controls";
export default function WalletPage(){const status=capabilityStatus();const privy=Boolean(process.env.NEXT_PUBLIC_PRIVY_APP_ID);return <div className="stack"><div className="statusline"><div><h1>Wallet</h1><p className="muted">Balances appear only after identity and onchain reconciliation.</p></div><button className="danger" disabled title="No active mandate">Pause mandate</button></div><section className="grid"><article className="panel"><h2>Personal wallet</h2><p className="balance">Unavailable</p><p className="muted">Controlled by you through Privy. Arclet never exports its key.</p>{privy?<SessionControls/>:<div className="notice">Add the Privy public and server credentials in setup.</div>}</article><article className="panel"><h2>Trading wallet</h2><p className="balance">Not assigned</p><p className="muted">Operated by Arclet through a dedicated Circle Agent Wallet. Funding transfers custody.</p></article></section><section className="grid"><article className="panel"><h2>Create a mandate</h2><label htmlFor="instruction">Trading instruction<textarea id="instruction" rows={4} placeholder="Example: Buy 1 USDC of cirBTC daily, subject to the visible limits." disabled={!status.configured}/></label><p className="muted">No market is enabled until Graph and Circle probes pass. Unsupported requests are never silently changed.</p><button disabled>Review exact rules</button></article><article className="panel"><h2>Controls</h2><div className="stack"><button className="danger" disabled>Pause without AI</button><button className="secondary" disabled>Withdraw to personal wallet</button></div><p className="muted">Withdrawal supports the verified linked Privy wallet only. It never swaps assets implicitly.</p></article></section><section className="panel"><h2>Active mandate</h2><div className="empty">No mandate is active. Strategy activation requires a verified wallet, enabled market, immutable draft, and Privy signature.</div></section></div>}
