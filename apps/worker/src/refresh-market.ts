import { canonicalHash } from "@arclet/domain";import { GraphClient,buildProvenance,normalizeSnapshot,snapshotDataSchema } from "@arclet/graph";import type { Sql } from "postgres";import { createPublicClient,http } from "viem";import { mainnet } from "viem/chains";import { z } from "zod";import registry from "../../../config/markets.json";
const enabledSchema=z.object({id:z.enum(["usdc-cirbtc","usdc-eurc"]),enabled:z.literal(true),marketConfigHash:z.string(),sourceChainId:z.literal(1),deployment:z.string(),pool:z.string(),sourceReferenceToken:z.string(),sourceUsdcToken:z.string()}).passthrough(),metaSchema=z.object({_meta:z.object({deployment:z.string(),hasIndexingErrors:z.boolean(),block:z.object({number:z.number(),hash:z.string().optional()})})});const metaDocument="query ArcletMeta { _meta { deployment hasIndexingErrors block { number hash } } }";
export async function refreshMarkets(sql:Sql<Record<string,never>>){const key=process.env.GRAPH_API_KEY,rpcUrl=process.env.SOURCE_RPC_URL,subgraph=process.env.GRAPH_SUBGRAPH_ID;if(!key||!rpcUrl||!subgraph)return;const rpc=createPublicClient({chain:mainnet,transport:http(rpcUrl)}),head=await rpc.getBlockNumber(),document=await Bun.file("packages/graph/src/queries/market-snapshot.graphql").text(),client=new GraphClient(`https://gateway.thegraph.com/api/subgraphs/id/${subgraph}`,key);for(const raw of registry.markets){const parsed=enabledSchema.safeParse(raw);if(!parsed.success)continue;const market=parsed.data,meta=await client.query(metaDocument,{},metaSchema);if(meta._meta.hasIndexingErrors||meta._meta.deployment!==market.deployment)throw new Error(`Graph metadata failed for ${market.id}`);const number=meta._meta.block.number,block=await rpc.getBlock({blockNumber:BigInt(number)}),data=await client.query(document,{pool:market.pool.toLowerCase(),poolKey:market.pool.toLowerCase(),block:number},snapshotDataSchema),now=Math.floor(Date.now()/1000),normalized=normalizeSnapshot(data,{sourceChainId:market.sourceChainId,deployment:market.deployment,pool:market.pool,referenceToken:market.sourceReferenceToken,usdcToken:market.sourceUsdcToken,maxBlockAgeSeconds:180,maxHeadLagBlocks:30,maxLastSwapAgeSeconds:600},{chainId:1,number,hash:block.hash,timestamp:Number(block.timestamp),headNumber:Number(head)},now),metrics={marketId:market.id,marketConfigHash:market.marketConfigHash,...normalized,sourceTvlUsdAtomic:normalized.sourceTvlUsdAtomic6,referencePriceUsdcAtomic:normalized.referencePriceUsdcAtomic6},provenance=buildProvenance({subgraphId:subgraph,deploymentId:market.deployment,sourceChainId:1,poolId:market.pool,sourceBlockNumber:number,sourceBlockHash:block.hash,sourceBlockTime:Number(block.timestamp),latestSwapTime:normalized.latestSwapAt,fetchedAt:now,queryName:"MarketSnapshot",queryDocument:document,variables:{pool:market.pool.toLowerCase(),poolKey:market.pool.toLowerCase(),block:number},normalizedMetrics:metrics,rawResponse:data});await sql`INSERT INTO market_observations (environment,provider,provenance,normalized_metrics,payload_hash,quality_verdict) VALUES ('arc-testnet','the-graph',${sql.json(provenance)},${sql.json(metrics)},${canonicalHash(data)},'HEALTHY')`}}
